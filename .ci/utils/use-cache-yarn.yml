steps:
- task: Cache@2
  inputs:
    key: 'yarn | "$(Agent.OS)" | "$(Build.SourcesDirectory)/npm-cli/yarn.lock"'
    restoreKeys: |
       yarn | "$(Agent.OS)"
       yarn
    path: $(yarn cache dir)
  displayName: Cache yarn
