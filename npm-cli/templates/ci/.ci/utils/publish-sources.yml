steps:
  - task: PublishBuildArtifacts@1
    displayName: '[Cache][Publish] Sources'
    condition: eq(variables['AGENT.OS'], 'Linux')
    inputs:
      pathToPublish: "$(Build.SourcesDirectory)/esy-sources"
      artifactName: 'esy-sources'
      parallel: true
      parallelCount: 8

